/* global Phaser */
import 'phaser';

/**
 * Usage notes:
 * For coordinating animations between a group of objects, each of which contains
 * a subgroup of objects. The animation 
 */
var WaveBounce = new Phaser.Class({
    prepareChildGroup: function(childGroup) {
        
    }
});

export {WaveBounce};